# Local Postgres (docker-compose provides this automatically for the app container).
DATABASE_URL=postgresql+asyncpg://wallet:wallet@localhost:5432/wallet
LOG_LEVEL=INFO
# Set to require when your managed Postgres mandates TLS (e.g. a public endpoint).
DB_SSL=
